{
  "name": "bloc-jams",
  "version": "0.1.0",
  "description": "A Spotify-style digital music player built with vanilla HTML, CSS, and JavaScript.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
